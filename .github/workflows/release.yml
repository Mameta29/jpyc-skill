name: Release

on:
  push:
    branches: [main]

concurrency: ${{ github.workflow }}-${{ github.ref }}

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  release:
    name: Publish to npm
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json
          registry-url: "https://registry.npmjs.org/"

      # The repo is an npm workspace; a single root install pulls both
      # @changesets/cli (root devDep) and the package's deps from the one
      # root lockfile. `npm install` (not `npm ci`) is used because `npm ci`
      # can skip platform-specific optional deps such as
      # @rollup/rollup-linux-x64-gnu (npm/cli#4828).
      - name: Install
        run: npm install --no-audit --no-fund

      - name: Pre-publish build + test
        run: |
          npm run typecheck
          npm test
          npm run build

      - name: Create release PR or publish
        uses: changesets/action@v1
        with:
          publish: npm run release
          version: npm run version-packages
          commit: "chore(release): version packages"
          title: "chore(release): version packages"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
